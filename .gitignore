# xmake
/.xmake/
/build/
# a standalone configure of the RHI component writes these beside its own root
/RHI/.xmake/
/RHI/build/

# fetched third-party (see `xmake setup`)
/ThirdParty/*
!/ThirdParty/README.md
/RHI/ThirdParty/
/Assets/Fetched/

# tooling
CLAUDE.local.md
/.cache/
compile_commands.json
*.gputrace*
*-dump/
*.bmp
imgui.ini
__pycache__/
.DS_Store
/.vscode/
/.claude/

# Written beside a golden file when a dump test fails, so the two can be diffed.
Tests/Golden/*.actual
