# R1.4 — Renderer Seams

**Status**: Implemented (2026-09-13)

Execute the [design](../specs/2026-09-13-r1.4-renderer-seams-design.md) after writing the
[milestone record](../milestones/r1.4.md). This plan covers only this slice.

## Integration

Use codex/r1.4-renderer-seams from local main; deliver one commit and fast-forward local main,
without a PR, as requested. This overrides the roadmap multiple-commit sequence and ordinary
PR convention for this slice. Preserve existing history; no remote publication is requested.
Subagents may independently freeze evidence, extract draw stages and separate record consumers.
Source changes wait for freshly built parent binaries and shaders to be frozen.

## 1. Freeze parent

Build App, Tests and TextureBake; preserve binaries, shaders, hashes, case inventories and commands
outside the source tree in /tmp/luminex-r1.4-evidence. Capture parent CPU/GPU/checkpoint results
and runtime artifacts from those immutable copies. Reuse prior evidence tooling with fresh inputs.

## 2. Extract concrete draw stages

Move shadow and scene pipelines, layouts, schema registration and per-object draw encoding into
ShadowStage and SceneStage. Renderer composes the stages and retains frame-wide orchestration.
Preserve creation errors, labels, pipeline descriptors, pass ordering, resources and arithmetic.
Do not edit shaders, uniform layouts, RHI interfaces or SceneView fields.

## 3. Separate compiled records

Extract the compiled record and required value vocabulary; update record consumers. Move shared
FrameDeclaration into Render and return the executed record for App-side retention. Update both
loops and integration tests. Move the unchanged nativeOnlyTemporalView predicate to Temporal
so model callers do not depend on the vendor stage header. Enforce the AppModel-to-RenderGraph
header restriction in the module checker and test direct/transitive rejection and leaf acceptance.

## 4. Validate

Build App, Tests and TextureBake. Run formatting, full policy including module link checks,
standalone Source/RHI headers, comments/layout, tooling tests, unit tests, Metal-validation GPU
tests and unchanged checkpoint-A filter. Compare old case names/tags and checkpoint selection.
Compare graph dumps, manifests/PNG metadata, capture schema, bake manifests/DDS/PNG bytes.
Compare captures semantically by pass/encoder/resource labels, ordering and uniform layouts,
excluding timings and editor wall-clock values; retain raw capture diagnostics.

Run the unchanged fifteen-case strict screenshot matrix in alternating parent/candidate order.
Fix a total maximum of eight paired rounds, including extensions, before measurement. Stop when
all candidate hashes have appeared for the same case in this session's parent runs. Preserve each
runner result and image. Do not change reference.json, strict runner semantics, the historical
M6.5 exception or baseline. At the cap, stop sampling: candidate-only hashes block merge until
attributed and fixed. Earlier slice exceptions do not authorize an exception here.

## 5. Record and integrate

Update current architecture, frame walkthrough, module convention, README directory table,
AGENTS.md and roadmap navigation. Record actual evidence and limitations in the milestone;
close the retained plan following the preceding slices. Review the diff and commit once as
`render: separate draw stages and compiled frame records`, then fast-forward local main.
No gate B or performance conclusion is implied.

## Execution result

The implementation, build, full policy, source-preservation review, CPU/checkpoint checks and byte/
capture/encoder comparisons pass. The initial system tracing interaction is reproduced in the
frozen parent and recorded in the [milestone](../milestones/r1.4.md#validation-tool-investigation).
Both frozen GPU suites pass with MTL_DEBUG_LAYER=1 under the same verified temporary non-Metal
foreground trace. The strict matrix passes its first paired round: both runners match all fifteen
historical hashes, with no exceptions or unresolved hashes. Sampling stops at one of eight rounds.
Original failures, recovered results, state probes and cleanup evidence remain intact. The slice
lands as one commit fast-forwarded into local main without a PR; this retained plan is closed.
Gate B still requires its own review and approval. No remote publication or performance claim.
