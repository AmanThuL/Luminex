# M6.5 — Display boundary and EDR evaluation

**Status**: In progress

## Authority and scope

Owner authorized execution on 2026-09-12 from the accepted
[design spec](../specs/2026-09-12-m6.5-display-boundary-edr-design.md).
[Roadmap](../roadmap.md#m65--display-boundary-and-edr-evaluation) owns the boundary.
Preserve display shaders, scene/exposure/temporal semantics, BMP bytes and all existing tolerances.
Production stays on `feat/m6.5-display-boundary-edr`; extended-range code stays on
`exp/m6.5-edr` and never merges into production.

## Delivery

- [x] A: explicit Render display domain, renderer accessor, CPU contract and real-display GPU
  fixtures with metadata-bearing PNG goldens; 1/255 comparison and exact reference hashes.
- [x] B: Engine PNG codec, extension-selected screenshots, PNG-default sequences with explicit
  BMP option, manifest v2 and comparison-tool v1 compatibility; strict reference parity runner.
- [x] C: encoded-space SDR UI declaration, read-only Inspector domain/density/1:1 status,
  startup log; verify existing Retina font rasterization before modifying it.
- [ ] P: isolated EDR probe and raw measurements; preserve source/evidence, record limits and
  adopt/defer policy without promoting experimental code.
- [ ] E: ADR, current guides, milestone evidence, commands and roadmap closure.

The domain is implemented first. Independent capture, tooling and UI work may be prepared in
parallel; production commits remain coherent and pass their applicable checks. The probe starts
from a verified production checkpoint in a sibling worktree.

## Validation and evidence

Build App and Tests explicitly; run CPU and GPU suites with Metal API validation for GPU work,
format check and policy including compilation-database-backed direct checkers. Test PNG CRC,
metadata, deterministic encoding and decoded BMP parity. Generate both real-display goldens and
compare against the existing oracle. Run the fifteen-image parity matrix and preserve every
mismatch rather than weakening hashes or tolerances. The accepted M6.4 record already documents
unexplained run-to-run drift; distinguish that limit from a proven new regression.

Record viewport backing density and settled 1:1 mapping, capture the UI, inspect font density.
EDR evidence includes platform headroom, float readback, timings, fallback and validation;
physical visible-range/white and photographs require an observer and cannot be inferred from
an SDR screenshot. Unavailable measurements remain explicitly pending, never fabricated.

## Current evidence

App and Tests build. All 580 CPU cases and 170 GPU cases pass (252,365 GPU assertions), with
Metal validation on. Formatting and policy pass. Both GPU fixtures match their oracle and golden
exactly on the reference machine; no tolerance or shader changed. UI reports 2× and settled 1:1;
the vendored font harness confirms actual 2× rasterization at the same logical font size.
The initial strict screenshot matrix matches 14/15: Sponza MetalFX scale 1 differs from the
historical hash. Four isolated baseline/current alternated reruns match the historical hash,
showing drift within the unchanged current executable. Keep the initial failure. PNG/BMP twins
match 13/15 independently rendered screenshots (the two Sponza vendor runs drift); all nine
paired tiny-sequence frames match. Both v2 formats and a rebuilt 360-frame v1 report pass.
The EDR probe is frozen at `m6.5-edr-evidence`, with validation-clean final smoke, ten samples
per pass/mode, stable float readback and a forced range-one check. The owner sees brighter EDR
highlights and has clarified that EDR white UI text is brighter than SDR at the same system
brightness. This supersedes the initial white-match impression and fails the intended UI-white
appearance check; no additional user comparison is pending.
[ADR 0019](../decisions/0019-display-domains-and-edr.md) proposes defer because of that mismatch,
the darkened midtones and incomplete production fallback/cost evidence.
The final full parity batch is 13/15, so literal hash-gate closure needs explicit disposition.
Raw results are under `~/Documents/Luminex-evidence/m6.5/` and the separate probe bundle.

The implementation uses a pure App `CaptureMetadata` serializer so manifest/frame facts are CPU
unit-tested without linking the windowed screenshot path. A and B form one verified production
commit because A's metadata-bearing goldens require B's PNG codec; the probe was prepared from
the spec commit in its own sibling worktree, copying the domain header, rather than blocking its
independent work on that codec cycle. No experimental source enters the production tree.

## Completion

Close only with roadmap/spec gates satisfied or an owner-authorized evidence exception.
Extract durable behavior, decisions, commands and limitations before removing this plan.
Do not advance the shipped baseline or mark ADR 0019 Accepted prematurely.
