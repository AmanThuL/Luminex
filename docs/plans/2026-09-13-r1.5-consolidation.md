# R1.5 — Consolidation and Decomposition

**Status**: Implemented (2026-09-13)

Execute the [design](../milestones/r/r1.5-design.md) after the
[milestone record](../milestones/r/r1.5.md). All five formerly optional groups are selected.

## Integration and order

Use `codex/r1.5-consolidation` from local main. Deliver one commit and fast-forward main without
a PR or remote publication. This owner instruction overrides the roadmap's separate-commit and
per-item-plan sequencing for this slice. Preserve existing local history. The milestone, design
and this plan are written in that order before implementation, following the preceding slices.
Independent agents may freeze evidence, split renderer/test responsibilities and reorganize build
and shader structure. Coordinate shared files and build access; no source edit precedes the parent
binary/shader freeze. Keep implementation arithmetic and declarations mechanically unchanged.

## 1. Freeze parent

Freshly build App, Tests and TextureBake, then copy binaries, runtime shaders and generated MSL
to `/tmp/luminex-r1.5-evidence/parent`. Record source revision, device/configuration, hashes,
commands, all case names/tags and checkpoint selection. Reuse evidence tooling with fresh inputs.
Retain both successful and failed validation runs. If the prior Apple tracing/debug interaction
recurs, reproduce on the frozen parent and use the same supported temporary non-Metal foreground
trace for both builds; record state probes and cleanup, without modifying validation or renderer.

## 2. Extract and decompose

Consolidate Core file, JSON, numeric, alignment and division helpers after auditing caller contracts.
Add focused tests for previously divergent contracts. Extract exposure/bloom/display, graph
compile/transition/validation, temporal native/upscale/vendor and panel canvas/details/dump units.
Split the five named test families by responsibility, preserving every existing case and tag.

## 3. Enforce visibility and move build/shader structure

Enumerate each unit's private headers and reject cross-unit reach with positive/negative checker
tests. Keep the migration allowlist empty. Move setup/rules/tasks to `xmake/` and target definitions
to module-local scripts. Move Slang modules/oracles, pass import search paths, widen dependency
tracking, reject basename collisions and add import-policy tests. Record twin rules in shader style.

## 4. Validate frozen candidate

Run clean App/Tests/TextureBake builds, format, full policy with link checks, standalone Source/RHI
headers, comment/layout checks, tooling tests and CPU/Metal-validation GPU/checkpoint suites.
Compare case names/tags and checkpoint selection with parent. Compare generated MSL with only
`#line` directives removed and unchanged metallib inventory/output locations. Verify incremental
module dependency rebuilds, restored shader output, isolated runtime-MSL fallback, one compilation
per AppModel source and Tests relinking after a library change. Freeze the final candidate.

Compare graph dumps, sequences/manifests/PNG metadata, capture-schema JSON and bake DDS/PNG/
manifests byte-for-byte. Compare GPU capture pass/encoder/resource labels, order and uniform
layouts, excluding timings and existing editor wall-clock fields. Retain raw diagnostics.

Run the unchanged fifteen-case strict image matrix, alternating parent/candidate order by paired
round, with a total maximum of eight paired rounds including extensions. Stop when every candidate
hash has appeared for the same case in this session's parent runs. Keep every image and original
runner result. No reference edit, widened exception, re-baseline or retry-until-pass is allowed.
At the cap, stop sampling and record any unexplained candidate-only hashes as integration blockers.

## 5. Record and integrate

Review the complete diff and extraction boundaries. Update architecture, frame walkthrough,
conventions, AGENTS, README navigation and roadmap to match the tree. Record actual evidence,
limits and any blocker in the milestone; close this retained plan following R1.1–R1.4's precedent.
Commit once as `core: consolidate helpers and implementation boundaries` and fast-forward local
main after all gates pass. Gate B remains separate; no performance claim or remote push.

## Execution result

All five implementation groups, source-preservation review, clean build/incremental/fallback
checks, format, full policy/link checks, tooling tests, CPU/GPU/checkpoint suites, byte artifacts,
semantic captures and detached-panel validation are complete. The
[milestone record](../milestones/r/r1.5.md#unresolved-image-parity) records the strict matrix failure:
at eight alternating paired rounds, two Sponza Native TAA 0.5 candidate hashes remain uncovered
by the same-session parent. Sampling has stopped; binaries, shaders and the strict runner remain
frozen. Extra trailing blank lines in eleven new Lua scripts were removed after freeze with
before/after hashes and an EOF-only equivalence proof, without altering any build instruction.

On 2026-09-13 the owner explicitly accepted the two-hash integration exception documented in the
[milestone](../milestones/r/r1.5.md#integration), closing this plan with one commit fast-forwarded
into local main. This is an explicit deviation from the planned image-parity gate: the comparison
remains failed and unexplained, without additional sampling or changes to references or runner
results. The exception applies only to this slice; gate B and later validation remain separate.
