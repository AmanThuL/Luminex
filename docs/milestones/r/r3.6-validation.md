# R3.6 — App validation

**Status**: In progress

[Part A PR #53](https://github.com/AmanThuL/Luminex/pull/53) is a draft with auto-merge disabled.

Part A implements the folder moves and the Inspector/Shell decompositions from the
[design](r3.6.md). Part A integration is blocked under the executor plan; Part B has not started. The
[R1 protocol](../../roadmap/codebase-module-boundaries.md#r1--module-boundaries-and-shared-foundations)
owns comparison. All results below name actual checked trees; no performance or
manual visual-acceptance claim follows.

## Revisions and evidence

- Frozen parent: `7b57102`; design: `232c324`; original executor plan: `7f97366`.
- Part A checked code: `feba9f5114919bf157ce1cbb07c26344cab1bfe2`; tree `09a09d4253c217fb73b3dc8ca1f5a8ec1f53d88c`.
  Subsequent Part A status changes are documentation only; integration is not approved.
- Raw evidence and scripts: `/tmp/luminex-r3.6-evidence/`; final local custody is
  `../Luminex-evidence/r3.6/`. Custody receipts determine actual copy verification;
  no Drive upload or cloud verification is claimed.
- Each gate records HEAD, staged tree, working-diff hash and binary/shader hashes.
  `move-ledger.json` and `a-tail-ledger.json` join these to committed trees.
  `moves.json`, `move.py`, `path-prose.py` and replay logs reproduce mechanical
  trees exactly. The 112-file move map has neither missing nor duplicate sources.

## Frozen baseline and gate controls

The parent passed the complete cheap gate and independent repeats of byte-stable
outputs before candidate collection. The baseline retains 902 Luminex name/tag
pairs in the default listing; a supplemental explicit `*` listing retains all 903,
including the hidden Sponza history diagnostic. Every one of the 23 A2–A24
commits was rebuilt and re-listed with explicit `*`; `full-inventory-retrospective-summary.json`
joins the supplemental manifests to each original revision/tree and manifest hash.
All retain the same 903 Luminex and 201 RHI name/tag pairs, with no additions.
`full-inventory-proofs/summary.json` rejects hidden-case removal/retagging and an
unapproved hidden addition. The original default listings remain unchanged.
The parent retains 201 RHI pairs, 202
generated MSL files with only `#line` removed, and five
contraction-disabled carriers: Frustum, LocalLightMath, Visibility, Occlusion and
LightClusters. CPU suites pass 702 cases / 1,035,388 assertions and 143 / 419.

`gate-proofs/summary.json` proves rejection of removed/retagged/unapproved tests,
removed/extra/duplicate flag carriers and a changed image byte; controls pass.
`appmodel-link/` tests the archive boundary against forbidden injected
symbol families and permitted Engine/RHI/Asset/standard-library references.

The cheap gate builds App, Tests, RojoRHITests, FrameDataBench and TextureBake;
compares MSL, identities and flags; runs both CPU suites, every root policy
checker, the two Python suites and format. The optimized driver only schedules
independent checks concurrently and retains every exit status. It does not cache
or omit validation. AppModel additionally passes raw and demangled archive-symbol
inspection for ImGui, SDL, Metal/MetalFX, bridge and Objective-C imports; neutral
RHI references remain allowed. Include-level policy remains the stronger boundary.

## Part A commit ledger

A2–A6 were checked before committing. A7–A21 were mechanically replayed before
committing, then their exact trees ran the same full gates in three isolated
external worktrees, all before PR review. This scheduling refinement was recorded
in the active plan before A7. The decompositions remained serial changes.

| Step | Original revision | Complete cheap gate |
|---|---|---|
| `A2` | `a33c0b69` | `A2` |
| `A3` | `8d9d82ee` | `A3` |
| `A4` | `b32d0bb2` | `A4` |
| `A5` | `4e92c33b` | `A5` |
| `A6` | `5fd29a5e` | `A6` |
| `A7` | `bca4fabc` | `V-A7` |
| `A8` | `b9a49ed2` | `V-A8` |
| `A9` | `9e815de7` | `V-A9` |
| `A10` | `8013aaa8` | `V-A10` |
| `A11` | `c5b1ae79` | `V-A11` |
| `A12` | `827f155e` | `V-A12` |
| `A13` | `3c1b714e` | `V-A13` |
| `A14` | `70fef258` | `V-A14` |
| `A15` | `2c35448e` | `V-A15` |
| `A16` | `9798d2da` | `V-A16` |
| `A17` | `f941034e` | `V-A17` |
| `A18` | `025314d4` | `V-A18` |
| `A19` | `27f5346c` | `V-A19` |
| `A20` | `8ad404ba` | `V-A20` |
| `A21` | `fe44357c` | `V-A21` |
| `A22` | `a7ec926d` | `A22` |
| `A23` | `30869226` | `A23` |
| `A24` | `feba9f51` | `A24` |

A2–A21 and A22 have identical mechanical replay trees. A23 and A24 pass the
per-code byte gate: Sponza, LightLab and TemporalLab with Temporal Off at frame 4,
plus a GPU/occlusion VisibilityLab control, Sponza frame-1 offscreen/editor graph
and tagged offscreen PNG. Tests remain flat; only App paths change in existing
include lines and path comments. Names, tags, strings and CLI surfaces are retained.

## App-specific observations

Every move touching Shell/Workspace passes a whole-file schema-3 workspace
roundtrip; isolated verifiers run this for every assigned move. A24 and the head
also load a real parent-produced nondefault save: 125% UI scale, visible
Performance/Render Graph windows and explicit detached bounds. Both candidate
re-saves are byte-identical. The original user workspace bytes are restored.

ImGui stores the current civil date in `LastUsed`. After midnight, the gate writes
a new date-scoped reference using the same hash-verified frozen parent executable
and proves its own fresh/reload equality first. It never changes old references
or normalizes candidate bytes. Collection crossing midnight fails and is retained.
`workspace-retrospective-validation-A-head.json` separately audits all 79 completed
workspace logs available at A24, including A2's original parent-reload log. Each
records Metal validation enabled and frame-loop completion, with no recorded
error, warning or assertion. Their byte hashes and the earlier audit are retained;
this retrospective scan is distinct from the later head run.

The schema-4 measurement gate compares keys, types, frame and pass-array counts;
run-varying values are excluded, so it supplies no timing/performance conclusion.
The Console gate compiles the actual panel and sink with deterministic timestamps
and observes ImGui text output across six levels, filtering, Freeze, Clear and
entry/byte eviction. It checks real sink forwarding and visible rows after Clear;
this is a scripted UI context, not an editor screenshot or manual visual review.

## Part A head

`head/A-head/` retains both binary identities, all raw reports and the fixed eight-round
alternating parent/candidate parity collection. Only candidate hashes absent from
all parent rounds fail; historical `reference.json` mismatches remain recorded.
No R3.5 exception, tolerance change, reference change or extra sampling applies.

The fixed 16 reports retain one candidate-only `sponza-taa-1` hash:
`e547ca6a9c2998797ce03cce104076111c3e126d8a0d4ab84ce5744a9b09f8bf`.
Compared with the shared parent/candidate hash
`a79841431e19c61af3afd24b4c37055342369432e7328655849b59674ea39a07`,
it changes 39 pixels by at most one 8-bit channel value. This fails the exact
hash-set gate; the small difference is descriptive evidence, not a tolerance.
The other 14 cases have no candidate-only hashes. Part A integration remains
blocked; Part B has not started. No additional parity rounds were sampled.

The head driver completed the independent GPU, screenshot, capture, workspace and
App gates, then its custom-task command `xmake policy -P .` failed because that
option is unsupported. The original failure remains in `policy-and-link.log`.
The root command `xmake policy` and direct `check_module_deps.py --link` were then
run successfully in `policy-corrected.log`. This command-only correction does not
change the renderer, the parity inputs or the failed parity result.

| Gate | Result |
|---|---|
| All per-commit checks and mechanical replays | Passed |
| Both GPU suites under Metal validation, on parent and candidate | Passed |
| Both checkpoint-A filters, on parent and candidate | Passed |
| Eight alternating temporal parity rounds | **Failed**, 14/15 cases satisfy the hash-set gate; one candidate-only Sponza TAA hash |
| Off screenshots, both graphs, tagged PNG and three-frame sequence/manifest | Exact |
| Strict capture schema, graph, encoder/resource labels and layouts | Exact |
| Default and nondefault workspace roundtrips; bounded validated editor | Passed |
| Measurement JSON structure and actual Console text | Passed |
| Full head policy and AppModel link inspection | Passed after a command-only policy invocation correction |

Source review independently confirms all 11 Inspector function definitions remain
byte-identical after extraction. Shell preserves 13 moved and 19 retained function
bodies by tokens, the settings handler ownership/registration-before-load boundary,
and docking/camera order. Final EditorShell is 768 lines; Inspector dispatch and
row helpers occupy 147. Private helper declarations add no public AppModel API.
`A-head-review.json` joins the final code revision/tree to a whole-Part-A review:
109 mechanical App files retain tokens and mapped include sets, all 41 changed
Test files retain tokens apart from includes/comments, and ten integrated
extraction artifacts match their reviewed hashes. The AppModel/App build covers
all 63 implementation files exactly once (33/30); contract changes are only moved
private-header paths and InspectorInternal. The review records no actionable
findings. Its author previously prepared Shell and the A25 documentation, so those
parts are an integrated cross-review rather than an independent-author review.

## Part B and limits

Part B has not started in the source tree. Isolated prevalidation rejects the proposed float
Core selection sphere: existing selection tests pass, but valid finite bounds at
±1e20 succeed in the double parent and fail after float radius overflow. The
planned adoption would retain the existing double fit; raw rejected candidate
and probe outputs remain in the evidence. This is a recorded adoption limit, not
an assertion that Core and App geometry are equivalent for all inputs.

Raw capture decoder limitations inherited from the parent are retained. Equal
capture schema/layouts do not claim complete uniform or texture decoding.
No source under RojoRHI, Render or Engine is changed. R3.7 remains inactive.
