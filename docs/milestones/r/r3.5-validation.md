# R3.5 — Render validation

**Status**: Implemented

Part A restructures Render's 82 original files under `Graph/`, `Renderer/`, `Common/` and ten
`Passes/` families, adopts four Core helpers, and decomposes Renderer, SceneStage and transition
derivation. Part B implements stage shape, setup and encoding helpers with the recorded adoption limits. The accepted
[record](r3.5.md) owns scope; the [R1 protocol](../../roadmap/codebase-module-boundaries.md#r1--module-boundaries-and-shared-foundations)
owns comparison. The tables record actual local results; PR integration state remains explicit. No performance claim follows.

## Revisions and evidence

- Parent: `2f37615`; design: `0422622`; implementation plan: `46ef4d7`.
- Part A checked code revision/tree: checked a6149108 and integrated 0486bfcc share tree c181a2aef4b27a1417d5505e9a6a9ce600cb2be3; A23 972261b changes documentation only; PR: [PR #51](https://github.com/AmanThuL/Luminex/pull/51), merged after green CI under the explicit owner parity exception below.
- Part B checked code revision/tree: checked a67fdd2d (tree 6229f28597737d39062eab8335f4697fd56468b0); integrated 1e5e877d (tree ec9881fb34b1a67232c7fa2234fc685f6b0fbdf9), with identical 635 non-documentation entries; PR: [PR #52](https://github.com/AmanThuL/Luminex/pull/52), delivery follows A on main; automatic merge is authorized after CI.
- Evidence was collected at `/tmp/luminex-r3.5-evidence/` against `/tmp/luminex-r3.5-parent/`.
  The verified local copy is `../Luminex-evidence/r3.5/`; checked runtimes and source snapshots
  are retained there after task worktree cleanup. Scripts, raw logs and reports stay outside source.
- `gate.sh` records HEAD, the staged tree, the working diff SHA-256, and binary/shader hashes.
  Pre-commit checks name the staged candidate tree; HEAD then names its predecessor.
  Mechanical commit-message rewrites preserve checked trees; the commit/tree ledger is reword-map.json, a-integration-map.json and replay-state.json; all A trees equal; b-integration-map.json preserves checked B source identities.

## Part A commit ledger

Each implementation tree was checked before its commit. The integrated revisions below retain
those exact trees; commit messages and parent IDs may differ from their isolated checked chain.

| Step | Integrated revision | Gate |
|---|---|---|
| A1 | `46ef4d7` | documentation policy |
| A2 | `22d032c6` | `A2`; mechanical replay |
| A3 | `b821f9a4` | `A3`; mechanical replay |
| A4 | `620ee69f` | `A4`; mechanical replay |
| A5 | `5a41c0d3` | `A5`; mechanical replay |
| A6 | `d2a9e8e6` | `A6`; mechanical replay |
| A7 | `464152bc` | `A7`; mechanical replay |
| A8 | `a8521552` | `A8`; mechanical replay |
| A9 | `6b00a68f` | `A9`; mechanical replay |
| A10 | `b7fc44d5` | `A10`; mechanical replay |
| A11 | `feab7bf7` | `A11`; mechanical replay |
| A12 | `682c73f9` | `A12`; mechanical replay |
| A13 | `89605798` | `A13`; mechanical replay |
| A14 | `9db534e0` | `A14`; mechanical replay |
| A15 | `d0bbbce6` | `pre-A15-candidate`; mechanical replay |
| A16 | `ae2e01c3` | `pre-A16`; byte gate |
| A17 | `9da11b31` | `pre-A17`; byte gate |
| A18 | `035da829` | `pre-A18`; byte gate |
| A19 | `5f12ecf3` | `pre-A19`; byte gate |
| A20 | `1b6ed9aa` | `pre-A20`; byte gate |
| A21 | `65601b2c` | `pre-A21`; byte gate |
| A22 | `0486bfcc` | `pre-A22`; byte gate |

## Frozen parent and gate verification

The parent and a separate parent-again run pass the cheap and heavy comparisons. `self-gate.out`
ends `GATE PASS baseline-self`; `self-heavy.out` ends `HEAVY PASS baseline-self`.

| Frozen fact | Result |
|---|---|
| Generated MSL | 202 files with `#line` records removed: 189 App/Tests/benchmark plus 13 standalone RHI |
| Luminex test inventory | 900 case names and name/tag pairs; 70 tag names |
| RHI test inventory | 201 name/tag pairs |
| CPU suites | Luminex 700 cases / 1,035,376 assertions; RHI 143 / 419 |
| Contraction-disabled carriers | Frustum, LocalLightMath, Visibility, Occlusion and LightClusters, exactly once each |
| Temporal Off screenshots | Sponza, LightLab and TemporalLab, four frames; two initial runs byte-identical |
| GPU visibility screenshot | VisibilityLab, GPU classification and occlusion on, four Temporal Off frames; byte-identical |
| Graphs | Sponza frame 1, offscreen and a fresh default editor workspace; parent-again byte-identical |
| Sequence | Three Sponza Native TAA PNGs plus manifest; parent-again byte-identical |
| Capture schema and semantics | Raw schema and graph byte-identical; 17 encoder labels in graph order, 104 resource labels, 17 layouts equal |

Inventory equality permits only exact name/tag additions listed before their first gate.
Part A adds `Stopwatch captured boundary stays fixed and shares adjacent intervals` with `[core]`.
The parent inventory stays frozen. `gate-proofs/summary.txt` records rejection of a removed or
retagged case, an unapproved case, removed/extra/duplicate flag carriers, one changed image byte,
a changed captured encoder label and a changed resource format. Unmodified controls pass.
Reordered object/layout registries fail raw schema equality and pass only part B's declared
creation-order comparison; their descriptors and upload order remain checked.

The initial A2 attempt exposed stale document links and an empty-source-glob warning that polluted
the generated compilation database. Both were corrected before A2's checked commit. A baseline
script edit while Bash was running caused a trailing parser error after its gate body completed;
an immutable script copy was rerun and passed. These unsuccessful attempts remain in the evidence.

## Part A

| Gate | Checked result |
|---|---|
| A1 docs; A2–A22 cheap gates; A2–A15 mechanical replay | passed; all replay trees equal; integrated A23 draft full gate also passed (701 CPU cases / 1,035,382 assertions; RHI 143 / 419) |
| A16–A22 per-code-commit byte gate | passed all seven; four BMPs, two graphs and tagged offscreen PNG exact (`pre-A16`–`pre-A22`) |
| Full Tests and RHI GPU suites under Metal validation | passed on both sides: Tests 200 / 1,160,477; RHI 58 / 7,416, validation clean |
| Both checkpoint A filters | passed on both sides: Tests 3 / 669; RHI 16 / 1,187 |
| Eight alternating parity rounds, fixed before collection | **failed**: one candidate-only hash for `sponza-vendor-0.5`; remaining 14 cases have none; sampling stopped at the cap |
| Head graphs, tagged PNG and three-frame sequence/manifest | passed exact comparisons in `heavy/head-pre-A-head` |
| Head capture schema, labels, encoder order, resource semantics and uniform layouts | passed strict raw schema and graph equality; 17 encoders, 104 resource labels and 17 layouts in `capture/head-pre-A-head` |
| Head full policy and independent review | passed full policy (152 standalone headers, 304 comment files, 2,060 definitions / 277 layout files); review retains the unresolved parity cause |
| PR A integration | merged after green CI with the explicit 2026-09-22 owner exception below; the strict parity gate remains failed |

Read-only review compared A22's nine major loops with their predecessor: bodies are token-identical
and orchestration retains texture/buffer seeding, then alias, RAW, WAW/WAR and access-recording
order. A17's resolved bounds agreed on 200,512 boundary/random triples, including zero counts,
all-count sentinels and uint32 saturation. These checks supplement the implementation gates.
A18 adds `Stopwatch::elapsedMillisecondsUntil` so adjacent published durations share the exact
original sample; the Core charter and dependency direction are unchanged.

The parity failure is retained in `head/pre-A-head`: round 5 candidate alone produced
`427b090f8668ec30a8436ba5ce1fca334f6207f8a03835bfdc683d76cd7620d4`; all eight parent and
the other seven candidate runs produced `2cd1bd1d48a85f0419692a346ccce2597ef0720d50e5ce3103d507355322990b`.
It changes 257,938 pixels with maximum channel delta 4. The cause remains unresolved; this is
not a metadata-only difference or an accepted tolerance. The R1 cap forbids additional sampling
to seek a pass. No reference, runner or round cap was changed. The failed gate originally blocked
integration; the owner explicitly accepted this single failure on 2026-09-22, as scoped below.
The independent head checks were completed after the parity driver stopped; their passes do
not turn the failed head into a pass. Source review records unchanged Temporal/Exposure/Bloom/
Display bodies and Renderer creation order in `A-head-source-review.json`.

## Part B

Stage shape and shared-helper implementation is complete. Scene retains `encodeDrawRuns` and all four stages retain `bindSceneTables`; Shadow keeps its explicit run loop after the fixed fallback selected the measured Scene-only candidate.

The same-binary one-pair/one-frame smoke validated invocation only and supplied no cost conclusion.
Scored B19 collection compares frozen B18 with the exact accepted candidate, excluding B20.
The fixed protocol uses 12 fresh-process AB/BA pairs, W32/N256, 10,000 bootstrap resamples,
seed `0x4C4D5836`, Sponza and VisibilityLab 16,384, direct/indirect/batched submission.
Both sides require schema 4 and identical plans; only executable provenance may differ.
A regression is an encodeMs baseline-minus-candidate absolute interval whose upper bound is below zero.
After combined failure, the predeclared isolated-candidate decision prefers Scene, then Shadow,
then reverts all call-site adoptions. Every retained combination is exactly a measured candidate.

Paired reports, cell intervals, decision and attempts: `B22-support/B19-summary.json`, `measure/B19-{both,scene,shadow}/analysis.json`, context manifests and `bench-runtime/*/freeze.json`.

| Gate | Checked result |
|---|---|
| B1 convention; B2–B21 and final Renderer correction | passed per-step gates, with B2–B20 and pre-B3-review byte comparisons; b-chain-commits.json, b-tail-commits.json, b-final-commits.json, b-finishing-commits.json and b-integration-map.json retain exact identities |
| Existing identities, additive tests and CPU suites | 900 existing case/tag pairs retained; only Stopwatch and Dispatch additions; CPU 702 / 1,035,388 assertions, RHI 143 / 419 |
| Paired driver | visibility selftest and lighting compatibility selftest passed at B21 |
| Eight-round parity | passed all 15 cases with zero candidate-only hashes across 16 complete reports; historical reference failures retained; no additional A sampling |
| PR integration | owner authorized both PRs to auto-merge on 2026-09-22; B follows merged A on main, retaining the checked non-documentation tree |
| Parent/candidate GPU suites and checkpoint A | passed under Metal validation: Tests 200 / 1,160,477 assertions; RHI 58 / 7,416; checkpoint Tests 3 / 669 and RHI 16 / 1,187 |
| Head images, sequence and graphs | passed byte-exact comparisons for four BMPs, both graphs, tagged PNG and three-frame sequence/manifest (`heavy/head-pre-B-head`) |
| Head capture | passed the declared object/layout registry-order comparison only: 17 encoders, 104 resource labels and 17 layouts; exact graph and unchanged decoder limits (`capture/head-pre-B-head`) |
| Final policy and review | passed full policy: 785 repository files, 63 shaders / 77 imports, 452 module files / 10 units / one external component, 158 headers, 311 comment files, 2,058 definitions / 279 files; independent read-only review found no actionable defect, without fault injection |

### B19 measured adoption

The frozen protocol collected all 18 cells: 12 fresh-process AB/BA pairs per cell, W32/N256,
10,000 paired-median bootstrap resamples, seed `0x4C4D5836`, 95% intervals. The baseline is
B18; B20 is excluded. Values below are paired `encodeMs` differences in milliseconds,
baseline minus candidate: positive means less candidate cost. A wholly negative interval
fails adoption. Intervals that include zero establish no improvement; this is no throughput claim.

| Adoption | Workload | Submission | Median Δ ms | 95% interval ms | Regression |
|---|---|---|---:|---|---|
| both | sponza | direct | -0.010583 | [-0.023708, +0.021938] | no |
| both | sponza | indirect | -0.000396 | [-0.012687, +0.004864] | no |
| both | sponza | batched | -0.007604 | [-0.024041, +0.013063] | no |
| both | visibility-16384 | direct | -0.058604 | [-0.153740, -0.011104] | yes |
| both | visibility-16384 | indirect | -0.103709 | [-0.173271, +0.079302] | no |
| both | visibility-16384 | batched | -0.009990 | [-0.025813, +0.002208] | no |
| scene | sponza | direct | -0.003761 | [-0.011312, +0.022271] | no |
| scene | sponza | indirect | -0.009136 | [-0.035875, +0.011855] | no |
| scene | sponza | batched | -0.001083 | [-0.032448, +0.006573] | no |
| scene | visibility-16384 | direct | +0.090051 | [+0.007063, +0.163646] | no |
| scene | visibility-16384 | indirect | +0.081146 | [+0.011906, +0.197771] | no |
| scene | visibility-16384 | batched | +0.004552 | [-0.001292, +0.016541] | no |
| shadow | sponza | direct | +0.003198 | [-0.014729, +0.023386] | no |
| shadow | sponza | indirect | +0.008000 | [-0.001500, +0.020708] | no |
| shadow | sponza | batched | -0.017677 | [-0.030532, -0.004208] | yes |
| shadow | visibility-16384 | direct | +0.068406 | [-0.011125, +0.131730] | no |
| shadow | visibility-16384 | indirect | -0.016083 | [-0.096219, +0.057011] | no |
| shadow | visibility-16384 | batched | +0.002073 | [-0.002917, +0.022615] | no |

The combined candidate fails VisibilityLab/direct; isolated Shadow fails Sponza/batched.
The complete Scene-only candidate passes all six adoption checks and is selected by the
predeclared fallback. Scene retains `encodeDrawRuns`; Shadow retains its explicit run loop.
All four stages retain `bindSceneTables`; SelectionOutline and OcclusionReference keep their
independent direct-item loops. No unmeasured combination is inferred to pass.

Raw reports remain under `measure/B19-{both,scene,shadow}/`; their context and frozen-runtime
manifests retain binary/tree identity, asset/shader/dylib hashes and process snapshots.
The device was Apple M3 Max on macOS 26.7, release build. Ordinary background activity was
not controlled. These costs do not resolve or replace either PR head image gate.

The retained Scene-only source tree is exactly the measured tree
`cb2e0376c7ae7641f221b4dda9275be4e6e598a7`. Its gate rebuild App SHA-256
`22cc20fca7c8a0f78d236f56b7cd40bcb9de312200b40301a2670f99d3069562` differs from the
frozen measured binary `d502d019054a955ad306c299c7458488184f90a0f84d3c0f369f535dcf2e1332`.
`B19-measured-gated-binary.json` retains the hash comparison. The gated executable was not
copied before B20, so this evidence supports no byte-level diagnosis or UUID/metadata cause.
No rescoring followed. The exact measured source-combination rule holds; the costs above apply
only to the frozen measured binary, with no cost claim for the gated rebuild or later B20.

## Owner acceptance and integration

On 2026-09-22 the owner explicitly instructed: “接受A为exception，两个都auto merge”.
This accepts only Part A's retained round-5 `sponza-vendor-0.5` candidate-only hash as an
integration exception and authorizes automatic merge of both PRs after CI. It does not resolve
the cause or change the failed strict gate into a pass. There is no expanded tolerance,
reference change, extra sampling, manual visual-review claim or new performance adoption.
The general R1 protocol and its future-slice requirements remain unchanged.

- Part A: **strict parity failed, owner-accepted for integration**; the original hash, pixel
  difference and fixed eight-round evidence remain above. PR #51 merged after green CI.
- Part B: **local gates passed**; its independent eight-round parity, GPU/checkpoint, exact
  images/graphs/sequence and declared capture comparison passed. Full policy is recorded in
  `pre-B-head-standalone-policy-retry.log`. B does not reproduce or resolve A's unique hash.
- Both published source heads had passing hosted CI before final integration. B is based on
  merged A with only closure-documentation changes and identical checked non-documentation
  entries. [PR #51](https://github.com/AmanThuL/Luminex/pull/51) and
  [PR #52](https://github.com/AmanThuL/Luminex/pull/52) retain the final hosted checks and merge state.

The implementation plan is closed and removed; its history remains in Git. The milestone is
Implemented with the scoped acceptance above. The local evidence copy contains 15,551 verified
entries; `RECOVERY.md`, `custody.py` and `.custody/manifest.json` preserve runtime/source recovery.
Integration receipts and owner authorization are appended under `.custody/` without changing
frozen raw evidence. The group remains local/untracked; no Drive upload or cloud verification
is claimed.

## Capture decoder limits

The frozen parent dump contains an existing `missing-expected-resource` error because its anomaly
rule still expects `lmx.render.sceneColor` while the schema names `lmx.render.sceneColorHdr`.
Unsupported formats remain undecodable, and uniform blob attribution reports six candidates and
stays unresolved. The candidate must preserve these findings; equal layouts are established by
the schema and equal encoder order by decoded CS/setLabel records joined to the graph schedule.
The capture gate does not claim complete texture or uniform-value decoding. Capture paths, blob
object IDs, timings and pixel statistics are excluded from semantic comparison; raw schemas and
graphs remain byte gates in part A. Part B alone may canonicalize object/layout registry order as
the design declares; no label, descriptor, uniform layout or upload order is dropped.
