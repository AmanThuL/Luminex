# M7.4 — Implementation and Validation Record

**Status**: In progress

The owner authorized execution on 2026-09-18 after the [design](m7.4.md) was written.
The [implementation plan](../plans/2026-09-18-m7.4-conservative-occlusion.md) preceded source changes.
This records implementation and completed automated verification of frozen candidate 1. Native
editor/Xcode verification and owner review remain open. No Superpowers skill or gate waiver is used.

## Evidence and frozen artifacts

Evidence root: `~/Documents/projects/Luminex-evidence/m7.4-2026-09-18/`.
All evidence names below are relative to that directory. Raw reports, binaries, shaders, source
snapshots, logs and failed attempts stay outside the source tree. The evidence migration is
independent of this collection; historical bundles have not been rewritten or deleted.

`parent/provenance.json` identifies source `cc2a7af` and renderer parent `147206e`.
`candidate-1/provenance.json` identifies branch `codex/m7.4-occlusion`, base `cc2a7af`,
and 732 source files. `candidate-1/source-hashes.json`, `source.patch` and `source.tar.gz`
preserve the working source. Runtime source is frozen during collection.

| Artifact | SHA-256 |
|---|---|
| Parent App | `3c6c5bb82c75b2be17e3386f7680e34669d3e08d2afb0940249aa29100bc45e1` |
| Candidate 1 App | `568d85bb3fa4c99913954cb7071d4b39352a0b74bcdb357d4a3ee1e37f29b114` |
| Candidate 1 Tests | `8ff5fcb87de21b866aaeeb1d18115b5ebf92311ee7fd29b05a2400b6032bab45` |

The later all-targets relink produced App
`6fe7c015f162a7c39e1bc4bccfcf081f11d0582e086bf73ee66413346c0d0269`.
`post-all-targets-build-custody.json` and `post-all-targets-diagnostic-lines.json` retain the
comparison: 33 MOVZ diagnostic line-number immediates associated with
`Source/Scene/SceneTables.cpp` increased by one; all other section bytes and all shaders match.
Runtime source hashes remain unchanged. The frozen measurement App above was not replaced.
`final-runtime-custody.json` verifies frozen binaries and all 126 App shader files. Current Tests
is `9177a79bf23801283cfa0ecb5d02891648344b538846b90e453a214fc362d22e`.
Its 58 rebuilt metallibs differ from the earlier frozen Tests set; all 63 MSL files and five HZB
metallibs match. The current complete GPU rerun passes 224 cases / 880,307 assertions with Metal validation;
`hzb/final-current-gpu1.json` pins its binary/shaders. Earlier runs remain separate; no binary
equivalence is claimed. Bench target artifacts are inventoried separately.

The recorded device is Apple M3 Max (40 GPU cores, 64 GB unified memory). Measurement reports
identify release builds and Darwin 25.6.0. Static, rail and image-matrix runs recorded here use
`MTL_DEBUG_LAYER=1`; their costs are unscored. Raw reports retain shader hashes and instrumentation.
These correctness runs cannot establish performance or default adoption.

## Four independent gate families

| Family | Current finding |
|---|---|
| Per-level correctness | PASS in the completed exact CPU/GPU fixtures and graph checks |
| Strict visibility | Static ID checks PASS; same-binary exact image gate FAILED, 13/15 |
| Motion latency and recovery | 24 complete rails and synchronized image pairs; maximum missing streak one; images differ |
| Performance | COMPLETE: 24 paired cells; mixed costs, no default adoption |

### 1. Per-level correctness

`hzb/hzb-tests-attempt1.log` records 334 assertions in three cases passing with Metal validation.
The per-level oracle compares each GPU level directly against source depth coverage. The fixtures
cover full and half render scales, odd edges, clear depth, masked raster coverage and active-scale
reuse without reallocation. CPU fixtures pin allocation and R32Float capabilities. The HZB gate
was completed before production occlusion consumers were enabled.

The complete GPU suite includes the occlusion graph golden. The graph roots every mip and the
final publication read, preserving the previous pyramid until classification finishes. The
synthetic-pyramid CPU/GPU probes cover outcomes, source rectangles, levels and depth bits, with
guard-boundary probes exercising retained uncertainty. The additional actual raster-built
HZB/CPU box comparison in `GpuHzbOcclusionTests.cpp` was compiled after `full-gpu1.log`. It passes
in the combined actual-HZB, integration and unchanged checkpoint-A focused run with Metal
validation: **24 cases / 9,083 assertions**, retained in
`hzb/actual-parity-integration-checkpoint1.log`. This later run is separate from that suite result.
`hzb/occlusion-tests-attempt1.log` records the earlier focused 12-case / 6,257-assertion pass.
The current 224-case complete GPU rerun below now also includes the added fixture.

### 2. Strict visibility

`static-attempt1/report.json` contains nine complete runs. Each uses GPU batched submission,
`--occlusion on --occlusion-check --classify-check --unscored --warmup 0 --frames 32`
and `--measure-camera initial`, at 1280×720 output. VisibilityLab uses 1,024 instances and eight
opt-in occluders; Sponza and San Miguel use their catalog geometry.

| Scene | Temporal / scale | Runs | Falsely rejected visible instances / affected pixels |
|---|---|---:|---:|
| Occluded VisibilityLab | Off / 1.0, Native TAA / 1.0, Native TAA / 0.5 | 3 | 0 / 0 |
| Sponza | Off / 1.0, Native TAA / 1.0, Native TAA / 0.5 | 3 | 0 / 0 |
| San Miguel | Off / 1.0, Native TAA / 1.0, Native TAA / 0.5 | 3 | 0 / 0 |

All 288 reference records are present and pass, with no invalid ID pixels or unmatched candidates.
Each run has 31 strict-history frames after its initial invalid-history frame. Independent review
of the raw samples found unique frame IDs, exact visibility/frame joins, zero overflow and zero
state/row/argument/counter mismatches. The lab actually rejects up to 46–47 instances per frame;
Sponza Off rejects none at this initial camera, so that cell alone does not exercise rejection.

GPU integration fixtures cover exact static HDR/depth/motion/reactive attachments, newly visible
geometry, global invalidation and overlapping retirement under indirect and batched submission.
Invalidation sections exercise occluder movement/reshaping/masking/removal, hidden-object movement,
addition, scene identity, explicit cuts, translation/rotation jumps, resize, toggle and source-frame
gaps. `hzb/verification-integration2.log` records 1,170 assertions in four cases passing.
Wireframe is conservatively retained globally and the independent reference shares its coverage;
`hzb/verification-wireframe1.log` records seven cases / 3,610 assertions passing.

The same-binary fifteen-image matrix has a stricter, separate result. Both sides use candidate 1,
GPU classification and 32 rendered frames; only occlusion changes. The exact SHA-256 gate in
`candidate-matrix-attempt1/report.json` passes **13/15 and FAILS overall**.

| Exact-image failure | Differing RGB pixels | Percentage | Maximum channel delta | Pixels with delta > 8 |
|---|---:|---:|---:|---:|
| Sponza Native TAA / 0.5 | 71 | 0.007704% | 3 | 0 |
| Sponza MetalFX / 0.5 | 83,787 | 9.091471% | 5 | 0 |

`occlusion-image-differences.json` quantifies these differences. Its generic threshold comparator
passes 14/15, but that threshold result cannot replace the required exact 13/15 result. The
inherited Sponza TAA divergence is neither a waiver nor an established cause. No guard or image
threshold was changed in response to these measurements.

The parent-versus-candidate **default-path** comparison is distinct. Both
`default-strict-comparison-attempt2.json` and
`default-vendor-quantization-v1-comparison-attempt2.json` pass 15/15 under their recorded M7.1
criteria. Fourteen image hashes match exactly. Sponza Native TAA / 0.5 differs at 75 pixels
(0.008138%), maximum channel delta 3, with no delta above 8; it passes the recorded threshold.
The scoped vendor profile applies only to its vendor cells and grants no same-binary exception.

`probes-analysis-attempt1/report.json` records four raw TemporalLab frame-32 RG16Float motion
comparisons: parent/candidate CPU defaults and candidate GPU occlusion off/on, at scales 1/0.5.
All bytes, finite masks and invalid sentinels match; maximum UV delta is zero (bound 1/4096).
The parent/default Native TAA / 1.0 sequence (W32, three saved frames) has zero differing pixels
and zero LDR-FLIP throughout; its synchronized manifest and hashes are in that directory's
`parent-sequence/comparison.json`.

Instrumented Sponza Native TAA / 0.5 compares all 32 frames. Depth/motion/reactive match throughout;
HDR differs only at frame 22 (606 pixels, maximum component delta 0.060302734375), and display at
frames 22–32 (2,966 pixel observations). An off repeat matches on for every attachment/frame.
`attachment-taa.json`, `attachment-repeat-taa.json` and `taa-hdr-frame22-details.json` retain this
same-mode variability; its cause is unresolved. Fresh BMP off/on pairs match for TAA and MetalFX;
TAA on/on-repeat differs at 78 pixels (maximum delta 3), while off/off-repeat and every fresh
MetalFX repeat match. Original-versus-fresh on still differs at 71 TAA / 83,787 MetalFX pixels.
These repetitions neither replace the original **13/15 FAIL** nor establish its cause.

Two original MetalFX attachment captures failed in the external harness's unnecessary read/read
`ExternalRead` / `ShaderRead` barrier assertion. The failures, source and binary remain in
`probes-attempt1/` and `attachment-harness-attempt1/`. A harness-only fix leaves production unchanged.
`AttachmentProbe-attempt2` completed all three MetalFX / 0.5 captures under Metal validation:
`attachment-retry-attempt2/` records byte-exact HDR/depth/motion/reactive/display off/on and
off/off-repeat for all 32 frames, including finite masks. Its `comparison-off-on.json`,
`comparison-off-repeat.json` and `comparison-summary.json` retain metrics and custody.
No repetition grants a threshold exception or erases the failed attempt.

### 3. Motion latency and recovery

`rails-attempt1/report.json` is finished with all eight base runs complete. Each run contains
720 frames at 60 Hz, GPU batched submission, Native TAA, camera track, both checks enabled and
zero warmup. VisibilityLab adds eight opt-in occluders; San Miguel uses its catalog geometry.
The streak histogram counts missing instance observations, not distinct lifetime identities.
Affected pixels below are sums across frames, not unique screen pixels.

| Scene / lab instances | Scale | Frames with false rejection | Missing instance observations, streak 1 | Affected pixel observations | Maximum streak |
|---|---:|---:|---:|---:|---:|
| VisibilityLab / 1,024 | 1.0 | 27 | 78 | 70,301 | 1 |
| VisibilityLab / 1,024 | 0.5 | 31 | 85 | 24,618 | 1 |
| VisibilityLab / 16,384 | 1.0 | 658 | 9,900 | 796,497 | 1 |
| VisibilityLab / 16,384 | 0.5 | 654 | 11,144 | 198,352 | 1 |
| VisibilityLab / 65,536 | 1.0 | 517 | 58,909 | 1,727,846 | 1 |
| VisibilityLab / 65,536 | 0.5 | 516 | 47,175 | 424,841 | 1 |
| San Miguel | 1.0 | 0 | 0 | 0 | 0 |
| San Miguel | 0.5 | 0 | 0 | 0 | 0 |

Independent raw-sample audit in `hzb/base-rails-audit.json` verifies all eight report hashes and
App identities, all 5,760 frame/visibility joins, adjacent source frames, reference totals and
per-identity missing streaks. Every reference check is present and passes, with zero invalid ID
pixels, unmatched candidates, overflow or classification mismatches. Scene counter partitions
reconcile and every shadow candidate is retained. No missing streak reaches two. Nonzero one-frame
omissions are observed behavior, including on most frames of the 16,384-instance rail; these runs
are not described as zero false occlusion.

Each 65,536-instance run has 527 valid-history frames, 192 globally invalidated camera-translation
frames and one initial no-source frame. The invalid frames correctly perform no occlusion tests
and retain all frustum-surviving candidates. The other six runs each have 719 valid-history frames
and one initial no-source frame. Thus the largest rail's bound includes substantial global
retention, not 719 frames of active occlusion testing.

`rails-step-attempt1/report.json` completes eight more 720-frame rails, forcing the active scale
at sequence frame 360. This tests a forced extent step, not a dynamic-resolution controller
decision. Each row reports the same observation units as the base table.

| Scene / lab instances | Scale step | False-rejection frames | Missing observations | Pixel observations | Maximum streak |
|---|---|---:|---:|---:|---:|
| VisibilityLab / 1,024 | 1.0 → 0.5 | 28 | 79 | 36,607 | 1 |
| VisibilityLab / 1,024 | 0.5 → 1.0 | 30 | 84 | 58,312 | 1 |
| VisibilityLab / 16,384 | 1.0 → 0.5 | 657 | 10,508 | 408,213 | 1 |
| VisibilityLab / 16,384 | 0.5 → 1.0 | 655 | 10,559 | 586,674 | 1 |
| VisibilityLab / 65,536 | 1.0 → 0.5 | 516 | 53,002 | 931,391 | 1 |
| VisibilityLab / 65,536 | 0.5 → 1.0 | 517 | 53,146 | 1,221,377 | 1 |
| San Miguel | 1.0 → 0.5 | 0 | 0 | 0 | 0 |
| San Miguel | 0.5 → 1.0 | 0 | 0 | 0 | 0 |

`hzb/step-rails-audit.json` independently verifies all 5,760 raw frame/ID/source joins, counters,
report hashes, exact 360-before/360-after active extents, and per-identity streaks. History remains
valid at sequence frames 359/360/361 in every run. All checks pass with zero invalid IDs, unmatched
candidates, overflow or classification mismatches; no streak reaches two. The 65,536-instance runs
again have 192 camera-translation invalidations each, as disclosed for the base rails.

Recovery conditions map to independent fixtures: (a) source adjacency and deliberate frame gaps
in `OcclusionTests` / `GpuOcclusionTests`; (b) the production static-attachment fixture cycles
all 16 jitter phases across full/half/full extents; (c) CPU/GPU projection probes pin source-edge
rectangles and actual HZB tests check sampled coverage; (d) direct-depth per-level oracles;
(e) exact equality and guard-boundary probes retain uncertain boxes. The newly-visible production
fixture verifies rejection on the first visible frame and drawing on the second. The newly added
actual-HZB fixture and strengthened invalidation-following-frame assertions pass in the combined
24-case / 9,083-assertion focused run recorded above.

`sequence-summary.md` and its provenance record complete **16 synchronized pairs × 720 frames**
from `sequence-analysis-attempt1/`: 11,333 differing frames and 124,473,101 RGB-pixel observations.
Across all frame means, mean FLIP is 0.001622740 and nearest-rank P95 is 0.007132704; the maximum
frame mean is 0.216930360 at ordinal 709 (65,536 / 1.0). That image has 223,237 differing pixels;
visual inspection shows a large missing region. The independent matching rail reports 1,091
falsely rejected visible instances / 223,293 pixels, maximum streak one, at that ordinal.
`image-inspection/` retains lossless off/on PNGs, reference context and a 16,384-instance review
video (lossy convenience only; metrics use BMP originals). San Miguel also has nonzero image
differences despite zero reference-ID misses. No image threshold applies, and the geometry bound
does not establish image convergence. Actual controller-driven evidence follows.

`controller-summary.md` / `controller-summary-provenance.json` add eight on rails and eight exact
scale-schedule replays, all 720 frames, plus eight synchronized FLIP comparisons. The production
controller consumes real retired GPU times; only its budget switches at frame 360 (1000/0.001 ms,
reversed for initial 0.5). All runs make ten actual scale changes and reach the opposite endpoint.
The independent audit passes 11,520 timing joins and full per-identity reference checks; maximum
missing streak is one in the labs and zero in San Miguel. The largest runs again include 192
camera-translation invalidations. All pre/post custody inventories match. The external harness
uses pinned frozen source/libraries/shaders; reference/check costs and serialized retirement make
this a diagnostic, not editor realtime performance. Off replays the exact on extent schedule.
Per-run counts, controller ordinals and FLIP distributions remain in that summary. Nonzero image
differences persist, including in San Miguel. The first audit's Python compensated-sum mistake
and its exact sequential-add correction remain retained; no runtime change or tolerance was added.

### 4. Performance

`performance-summary.md` and its provenance inventory **24 complete cells / 576 raw reports**:
18 same-candidate off/on controls and six parent/default comparisons. Both collectors exit zero;
all cells retain 12 alternating pairs, W32/N256 and 10,000-resample 95% paired-bootstrap intervals.
`performance-execution.json` and start/end environment records retain commands and custody.
Instrumentation is disabled; AC power and no recorded thermal/performance warning are documented.
The original six workloads are unchanged; three opt-in occluded labs and indirect/batched controls
are separate. This is serialized-retirement pass cost, not throughput or end-to-end frame time.

Of 18 GPU pass-sum intervals, four indicate lower on costs (all indirect), twelve higher costs,
and two cross zero. The occluded 65,536 indirect paired off-minus-on delta is +1.854917 ms
[1.591594, 2.235948]; its batched delta is −2.249719 ms [−3.151229, 0.543552], not a stable gain.
HZB plus publication costs 0.049229–0.365885 ms across cell medians; pyramid allocation adds
2,621,280 bytes at 1280×720. The report separately preserves CPU classify/prepare/encode/wait,
exact GPU classify, full visibility, scene, shadow, HZB, pass sum, rejection counters and intervals.

Parent/default 65,536 CPU encode has paired parent-minus-candidate delta −0.286688 ms
[−0.532698, −0.221708]; the other five encode intervals cross zero. Coverage tracking is inside
this aggregate encode interval, but its isolated cost cannot be inferred. Zero-baseline metrics
retain absolute costs. Both `analysis-with-classify.json` files retain validated raw hashes and
unchanged original analyses; counters absent in the parent schema are not invented. No default
adoption, image waiver or realtime speedup follows from these observations.

## Regression and tooling inventory

| Check | Recorded result | Evidence |
|---|---|---|
| Release App build | PASS | `hzb/verification-app-build2.log` |
| Complete CPU suite, current Tests binary | PASS, 690 cases / 235,344 assertions | `hzb/final-current-unit1.log` |
| Complete GPU suite, current Tests binary with Metal validation | PASS, 224 cases / 880,307 assertions | `hzb/final-current-gpu1.log` |
| Python tool unit tests | PASS, 169 tests in current CPU suite; earlier 158-test run retained | `hzb/final-current-unit1.log`, `product/python-tools-unit.log` |
| Paired-driver selftest | PASS | `product/visibility-driver-selftest2.log` |
| Module / standalone source headers / RHI headers / comments / layout | PASS, explicit exit 0 | `hzb/final-policy-{modules,headers,rhi,comments,layout}2.log` |
| Shader imports | PASS | `hzb/final-policy-shaders1.log` |
| Format | PASS, explicit exit 0 | `hzb/final-policy-format2.log` |
| Project policy / whitespace | PASS after design status update; earlier status-syntax failure retained | `hzb/final-policy-project2.log`, `hzb/final-whitespace1.log` |
| Unchanged checkpoint-A filter with actual-HZB / integration fixtures | PASS, combined 24 cases / 9,083 assertions with Metal validation | `hzb/actual-parity-integration-checkpoint1.log` |
| Link policy | PASS, explicit exit 0 | `hzb/final-policy-link2.log` |

Earlier failed attempts remain visible: `hzb/verification-focused1.log` caught a null selected-object
diagnostics dereference; the null return was restored. `hzb/full-unit1.log` had two stale exact
CLI/manifest expectations; the subsequent complete CPU run passes. Build/graph bootstrap attempts
and attachment-probe compile attempts remain preserved. Harness and implementation failures are
not silently replaced by later passes.

## Editor, capture and handoff

Fullscreen-windowed validation of every catalog scene, selected-object retired-frame diagnostics,
source rectangle/bounds, all pyramid levels, UI invalidations and an unscored interactive check run:
**PENDING**. Automated fixture coverage does not replace the required real editor screenshots.

The maximized editor started under Metal validation and wrote `editor/occlusion.gputrace` at
frame 8 with GPU batched occlusion/check enabled. Its log records a 3348×2104 swapchain. Native UI
inspection returned “The Mac is locked”; manual unlock was requested. Real editor screenshots
and Xcode replay remain **PENDING**, so capture creation alone does not pass the replay gate.
`editor/occlusion-dump/manifest.json` retains the extractor findings: its legacy `sceneColor`
expectation is absent while `sceneColorHdr` exists, R32Float/other newer formats are unsupported,
and frame-data uniform blob joins remain ambiguous. These are not silently counted as passes.

Runtime custody and automated gate review are complete; `review-source/provenance.json` retains
this review commit and source archive. Native editor/Xcode checks and owner acceptance remain
**PENDING**. The 13/15 exact-image failure is open; no integration approval, default change,
plan closure or broader historical exception follows.
